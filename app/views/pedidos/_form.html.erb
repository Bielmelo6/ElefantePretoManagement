<%= form_with(model: pedido) do |form| %>
  <% if pedido.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
        <% pedido.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id%>
    <%= form.collection_select(:item_id, Item.all, :id, :produto,{:selected => @pedido.item_id})%>
  </div>

  <div class="field">
    <%= form.label :cliente_id%>
    <%= form.collection_select(:cliente_id, Cliente.all, :id, :nome,{:selected => @pedido.cliente_id})%>
  </div>

  <div>
    <%= form.label :quantidade, style: "display: block" %>
    <%= form.number_field :quantidade %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>